# T001 v0.42 Blocker Receipt

Result: external_blocker

The first-run onboarding child board produced draft PR #27 with local and
simulator proof, but it is intentionally not merged yet because its own oracle
requires physical iPhone proof before claiming beta-ready onboarding behavior.

Evidence recorded during the v0.42 child run:

- PR: https://github.com/mean-weasel/foil-ios/pull/27
- `scripts/ios-physical-harness.py status` found the expected harness pieces,
  but WDA was not reachable at `http://127.0.0.1:8100`.
- `xcrun devicectl list devices` showed `iPhone-preview` unavailable.
- Installing the draft branch onto a physical device is an operator action under
  this conveyor's stop rules.

Next owner: operator

Next action: make `iPhone-preview` available and unlocked, approve installing
PR #27's development build, and restore WDA or equivalent physical UI
automation before merging v0.42.
